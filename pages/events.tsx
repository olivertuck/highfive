import Head from 'next/head';
import Layout from 'components/Layout';

const Events = () => (
  <>
    <Head>
      <title>Events | Highfive</title>
      <meta name="description" content="Events | Highfive" />
    </Head>
    <Layout main={<></>} aside={<></>} />
  </>
);

export default Events;
